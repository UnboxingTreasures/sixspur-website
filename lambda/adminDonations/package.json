{
  "name": "sixspur-admin-donations",
  "version": "1.0.0",
  "description": "Admin view/manage donation records (view + status/notes edits only, no manual entry)",
  "main": "index.js",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}
